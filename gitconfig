[alias]
    ctags = "!sh -c '[ -f .git/hooks/ctags ] || git init; .git/hooks/ctags' git-ctags"
    diffgui = difftool --tool=meld
    filehist = log --patch --stat --follow
    ignored = ls-files --directory -o
    l  = log --date=relative --decorate --pretty=format:'%C(yellow)%h%C(reset) %s%C(bold red)%d %C(green)(%ad) %C(blue)[%an]%C(reset)'
    lg = log --date=short --graph --pretty=format:'%Cblue%h%Creset | %C(yellow)%ad%Creset | %Cred%an%Creset | %s%d'
    restore = checkout --
    sattus = status
    todo = grep --untracked -I --heading --break --line-number --fixed-strings --word-regexp \
        -e XXX -e TODO -e FIXME -e @bug -e @todo
        # per-file hits:  -c  (appendable on CLI)
    unstage = reset HEAD --
    undo = reset --soft HEAD^ --
    vimdiff = difftool --tool=vimdiff
    wdiff   = diff --no-ext-diff --word-diff
[blame]
    date = short
[color]
    ui = true
[core]
    excludesfile = ~/.gitignore_global
#    gitProxy = git-http-proxy
    pager = less -FRSX
[credential]
    helper = cache --timeout=3600
[diff]
    tool = vimdiff
    guitool = meld
[difftool]
    prompt = false
[diff "image"]
    textconv = identify
[diff "odf"]
    textconv = odt2txt
[diff "pdf"]
    textconv = ps2ascii
[diff "zip"]
    textconv = unzip -c -a
[github]
    user = yroyon
    token = fed6b8add696d67e89487310f418932a
[http]
#    proxy = bluecoat-be02.alcatel.fr:1080
#    proxy = http://www-proxy.exu.ericsson.se:8080
#    sslverify = false
[init]
    templatedir = ~/.git_template
[merge]
    tool = vimdiff
    #tool = meld
[push]
    default = tracking
[rerere]
    enabled = 1
[svn]
    rmdir = true
[user]
    name = Yvan Royon
    email = yvan.royon@gmail.com
